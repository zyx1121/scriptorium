---
description: "Ingest a source (file path, URL, or pasted text) into the active wiki — typically touches 5–15 pages."
argument-hint: "<path|url> [--collection <slug>]"
---

Load the `scriptorium` skill and follow the **ingest** flow in `reference/ingest.md`. Source: `$ARGUMENTS`

Run the connection-check first. Always discuss with the user before writing pages — silent ingests cause schema drift.
