---
description: "Ask the wiki a question. Returns a synthesised answer with inline citations."
argument-hint: "<question> [--collection <slug>]"
---

Load the `scriptorium` skill and follow the **query** flow in `reference/query.md`. Question: `$ARGUMENTS`

Run the connection-check first. If the wiki has no relevant pages, say so honestly — do not fabricate from training data.
