---
description: "Save durable insights from this session to the wiki before the conversation evaporates."
argument-hint: "[--collection <slug>]"
---

Load the `scriptorium` skill and follow the **recap** flow in `reference/recap.md`. Args: `$ARGUMENTS`

Scan the conversation, propose save candidates as a numbered list, draft each approved page, save via `scriptorium.create_page()`.
