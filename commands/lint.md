---
description: "Find rot in the wiki: orphans, dead wikilinks, missing concepts, stale claims, schema violations."
argument-hint: "[--collection <slug>] [--scope all|touched]"
---

Load the `scriptorium` skill and follow the **lint** flow in `reference/lint.md`. Args: `$ARGUMENTS`

Triage by severity, propose top 5 fixes, ask user approval per fix.
