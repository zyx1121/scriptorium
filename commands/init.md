---
description: "Create a new wiki collection on the connected Scriptorium server."
argument-hint: "<collection-slug>"
---

Load the `scriptorium` skill and follow the **init** flow in `reference/init.md`. Collection slug: `$ARGUMENTS`

Run the connection-check first (see SKILL.md). Abort early if not connected.
